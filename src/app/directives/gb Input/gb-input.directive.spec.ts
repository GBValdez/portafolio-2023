import { GbInputDirective } from './gb-input.directive';

describe('GbInputDirective', () => {
  it('should create an instance', () => {
    const directive = new GbInputDirective();
    expect(directive).toBeTruthy();
  });
});
